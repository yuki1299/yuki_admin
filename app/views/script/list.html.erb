<%= link_to "Roteiro"   , project_script_path(@project) %>
<%= link_to "Público"   , edit_project_public_path(@project, @public) %>
<%= link_to "Resultados", edit_project_public_path(@project, @public) %>

<div class="panel panel-info show-project-page">
  <div class="panel-heading">
    <p id="notice"><%= notice %></p>
  </div>

  <div class="panel-body">
    <p>
      <strong>Nome</strong>
      <%= @project.name %>
    </p>

    <p>
      <strong>O que vai testar?</strong>
      <%= @project.test_platform %>
    </p>

    <p>
      <strong>URL</strong>
      <%= @project.url %>
    </p>
  </div>

  <% @tasks.each do |task| %>
    <div class="panel-body">
      <p>
        <strong>Objetivo</strong>
        <%= task.objective %>
      </p>

      <p>
        <strong>URL</strong>
        <%= raw(task.url) %>
      </p>

      <p>
        <strong>Cenário</strong>
        <%= task.cenary %>
      </p>
    </div>
  <% end %>

  <% @asks.each do |ask| %>
    <div class="panel-body">
      <p>
        <strong>Objetivo</strong>
        <%= ask.ask_type %>
      </p>

      <p>
        <strong>URL</strong>
        <%= ask.question %>
      </p>
    </div>
  <% end %>
</div>
