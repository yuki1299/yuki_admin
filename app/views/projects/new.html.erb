<section id="new-test" class="user-bg">
  <div class="container-steps-content">
    <div class="nav-box">
      <ul class="user-steps-nav">
        <li <%= current_page?(new_project_path) ? "class=active" : "class=disable"%>>
          <%= link_to "Teste", "#" %>
        </li>
        <li <%= params[:controller] == "tasks" ? "class=active" : "class=disable"%>>
          <%= link_to "Tarefas", "#"  %>
        </li>

        <li <%= params[:controller] == "asks" ? "class=active" : "class=disable"%>>
          <%= link_to "Perguntas", "#" %>
        </li>

        <li <%= params[:controller] == "publics" ? "class=active" : "class=disable"%>>
          <%= link_to "Público", "#" %>
        </li>

        <li <%= params[:controller] == "credit" ? "class=active" : "class=disable"%>>
          <%= link_to "Créditos", "#" %>
        </li>
      </ul>
    </div>

    <div class="form-box">
      <div class="form-title">
        <h2>
          Conte-nos como será este teste
        </h2>
      </div> 
      <%= render 'form', project: @project %>
    </div>
  </div>
</section>
