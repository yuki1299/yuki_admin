<section id="new-test" class="user-bg">
  <div class="container-steps-content">
    <div class="nav-box">
      <ul class="user-steps-nav">
        <li class=<%= current_page?(new_project_path) ? "active" : "disable"%>>
          <%= link_to "Teste", "#" %>
        </li>
        <li class=<%=@project.persisted? ? "disable" : "locked"%>>
          <%= link_to "Tarefas", "#"  %>
        </li>

        <li class=<%=@project.persisted? ? "disable" : "locked"%>>
          <%= link_to "Perguntas", "#" %>
        </li>

        <li class=<%=@project.persisted? ? "disable" : "locked"%>>
          <%= link_to "Público", "#" %>
        </li>

        <li class=<%=@project.persisted? ? "disable" : "locked"%>>
          <%= link_to "Créditos", "#" %>
        </li>
      </ul>
    </div>

    <div class="form-box">
      <div class="form-title">
        <h2>
          Conte-nos como será este teste
        </h2>
      </div>
      <%= render 'form', project: @project %>
    </div>
  </div>
</section>
