<section id="user-register-step2">
  <div class="header-step2">
    <div class="img-box-mask">
      <div class="img-box">
        
      </div>
    </div>
    <h2>Cadastro de profissional</h2>
    <div class="title">
      <h3>Agora, alguns dados cadastrais</h3>
    </div>
  </div>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }, html: {class: "form-signin"}) do |f| %>

    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :full_name, required: true, autofocus: true, label: "Nome Completo", :class => "large" %>
      <%= f.input :role, required: true, autococus: true, label: "Profissão", :class => "small" %>
      <%= f.input :email, required: true, autofocus: true, label: "E-mail", :class => "large"%>
      <%= f.input :company, required: false, autofocus: true, label: "Empresa", :class => "small" %>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>

      <%= f.input :password, autocomplete: "off", required: false, label: false, placeholder: "Nova Senha" %>
      <%= f.input :password_confirmation, required: false, label: false, placeholder: "Confirme sua senha" %>
      <%= f.input :current_password, required: true, label: false, placeholder: "Senha atual" %>
    </div>

    <div class="form-actions">
      <button class="back-button">
        <%= link_to "Voltar", :back, :class => "btn btn-primary btn-block" %>
      </button>
      <button class="next-button">
        <%= f.button :submit, "Atualizar", :class => "btn btn-primary btn-block" %>
      </button>
    </div>
  <% end %>
</section>