<%= simple_form_for(@ask, html: {class: "form-signin"}, url: (params[:id].present? ? project_ask_path(@project, @ask) : project_asks_path)) do |f| %>
  <%= f.error_notification %>

  <%= f.input :ask_type, label: "Tipo de pergunta", required: true, autofocus: true, input_html: {class: "form-control"}%>
  <%= f.input :question, label: "Pergunta", required: true, autofocus: true, input_html: {class: "form-control"}, :as => :text%>

  <%= f.button :submit, :class => "btn btn-lg btn-primary btn-block", value: "Continuar para público", 'data-disable-with' => "..." %>
  <%= link_to 'Voltar', :back, :class => "btn btn-primary btn-block btn-lg" %>
<% end %>
